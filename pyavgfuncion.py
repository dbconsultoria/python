import pandas as pd
import numpy as np
import math

# Importar CSV
def avg(list):
    t = 0
    for n in lista:
        t += n 
        
    return t/len(lista)
    
lista = [1,2,3,4,5,6]
print(avg(lista))
